完整的webhook部署包括以下kubernetes资源部署：运行变异webhook服务器应用程序的单个实例。
它确保在节点失败的情况下至少有一个webhook实例始终运行webhook应用程序pod被重新调度到同一集群中的另一个节点。
在部署中运行webhook服务器还可以确保在单个群集节点重新启动和kubelet或容器运行时服务重新启动服务后应用程序将自动启动：向外部世界公开发生变化的webhook服务器应用程序，使其对kubernetes api server config可见map：定义包含webhook应用程序配置的文件。
webhook在每个许可审查请求时读取它，这使得配置文件在运行时可配置secret:contains tls certificate和private key，它们用于保护apl服务器和webhook应用程序之间的通信，从而更改许可控制器配置：enable t每当用户请求创建pod时，api服务器就会向webhook发送许可审查请求。
许可控制器配置指向服务端点，它还指定用于授权rbac规则的tls证书，并定义故障策略默认故障策略设置为忽略”，因此，如果webhook服务器出现故障，请启动新的pod并设置cmk binary和config volume mounts仍然可以在cluster init期间自动创建上述所有资源，尽管它们也可以使用位于resources/webhook目录中的模板规范文件手动部署。
手动部署需要预先创建一个正确签名的TLS证书和私钥对，并将其编码为Base64格式。
