要求1.
为给定的用户容器2提供对一个或多个物理核心的独占访问。
为用户容器组3提供对物理核心池的共享访问。
提供一种协作线程级CPU关联的形式：允许容器的一些线程在基础结构池中运行，而其他高优先级线程（例如。
g userspace poll mode driver）在独占分配的核心4上运行。
在未修改的kubernetes版本上运行支持的kubernetes版本：v1.5.x，v1.6.x 5。
允许从主机文件系统装载cmk工具，这样用户就不必在每个用户容器6中都包含这些工具。
在初始化cmk配置目录时，最好与isolcpus kernel参数进行良好的互操作，最好将独占cpu列表与完全隔离的物理核心7对齐。
